#  basic
---
- hosts: all
  gather_facts: False

  user: vagrant
  become: yes

  tasks:

    - name: Install python 2.7
      raw: >
        sudo bash -c "
        test -e /usr/bin/python || 
        (apt -qqy update && apt install -qy python-minimal python2.7)
        "
